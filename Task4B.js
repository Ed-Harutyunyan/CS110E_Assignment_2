//our coordinates
let x1 = 2;
let x2 = 2;
let y1 = 4;
let y2 = -3;

// equation for length
let length = Math.sqrt(Math.pow((x2-x1), 2) + Math.pow((y2-y1), 2));

// output of length
console.log(length);
